import ThemeCommand from '../../utilities/theme-command.js';
import { globalFlags } from '@shopify/cli-kit/node/cli';
import { startServer } from '@shopify/theme-language-server-node';
class LanguageServer extends ThemeCommand {
    async run() {
        const { flags } = await this.parse(LanguageServer);
        startServer();
    }
}
LanguageServer.description = 'Start a Language Server Protocol server.';
LanguageServer.flags = {
    ...globalFlags,
};
export default LanguageServer;
//# sourceMappingURL=language-server.js.map